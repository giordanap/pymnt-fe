name: react-candidate
services:
  web:
    container_name: react-candidate-web
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    # No runtime environment variables are required — the app fetches from a public API.
